name: Scrape

on:
  schedule:
    - cron: "0 */2 * * *"
  workflow_dispatch: {}

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      - run: npm run scrape:headless
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      # captureFailure() writes screenshot/DOM/aria-snapshot/error.txt to
      # data/_failures/ on any scrape failure - Telegram only gets the
      # screenshot, so upload the full bundle here for deeper debugging.
      # Runs even when the scrape step fails; no-ops if nothing failed.
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: failure-debug-${{ github.run_id }}
          path: data/_failures/
          if-no-files-found: ignore
          retention-days: 14
